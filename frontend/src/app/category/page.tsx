import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "Categories - Trendora",
};

const page = () => {
  return <div>page</div>;
};

export default page;
